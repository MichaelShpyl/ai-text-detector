apiVersion: v1
kind: Service
metadata:
  name: ai-text-detector
  labels:
    app: ai-text-detector
spec:
  type: NodePort
  selector:
    app: ai-text-detector
  ports:
    - name: http
      port: 80
      targetPort: 5000
      nodePort: 30080
